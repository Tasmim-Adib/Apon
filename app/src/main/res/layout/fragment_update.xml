<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        android:gravity="center_horizontal"
        android:id="@+id/lin"
        >

        <EditText
            android:layout_width="250dp"
            android:layout_height="40dp"
            android:id="@+id/last_blood_donation_edit"
            android:hint="Date of Last Donation"
            android:paddingLeft="10dp"
            android:drawablePadding="10dp"
            android:background="@drawable/custom_edit_text"
            android:inputType="text"/>

        <ImageView
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@drawable/calender"
            android:id="@+id/update_calender_icon"
            />
    </LinearLayout>
    <AutoCompleteTextView
        android:layout_below="@+id/lin"
        android:layout_width="290dp"
        android:layout_height="40dp"
        android:id="@+id/last_hospital_edit"
        android:hint="Where you donate last"
        android:layout_marginTop="15dp"
        android:paddingLeft="10dp"
        android:drawablePadding="10dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/custom_edit_text"
        android:inputType="text"/>

    <Button
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:text="Done"
        android:background="@drawable/custom_button"
        android:id="@+id/last_blood_donation_button"
        android:layout_below="@+id/last_hospital_edit"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        />


</RelativeLayout>